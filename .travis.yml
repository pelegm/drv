language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"

before_install:
  - sudo apt-get install -qq python-numpy python-scipy

before_script:
  - pip install coveralls

script:
  - pyb

after_success:
  - coveralls --verbose

